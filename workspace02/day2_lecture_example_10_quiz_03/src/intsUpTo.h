#ifndef INTSUPTO_H_
#define INTSUPTO_H_

#include <cstddef>
#include <vector>

std::vector<int> intsUpTo(std::size_t to);

std::vector<int> intsUpTo(std::size_t from /* = 0*/, std::size_t to);



#endif /* INTSUPTO_H_ */
